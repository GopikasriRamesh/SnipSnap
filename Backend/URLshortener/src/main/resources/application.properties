# === App Info ===
spring.application.name=SnipSnap

# === ? Neon PostgreSQL Database Connection (via Pooler + SSL) ===
spring.datasource.url=jdbc:postgresql://ep-restless-lake-a16r7s4t-pooler.ap-southeast-1.aws.neon.tech:5432/neondb?sslmode=require&channel_binding=require
spring.datasource.username=neondb_owner
spring.datasource.password=npg_1Wzc2jylqZGA
spring.datasource.driver-class-name=org.postgresql.Driver

# === ? Hibernate & JPA Settings ===
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.show-sql=true

# === ? Custom Base URL ===
# Use localhost for development
app.base-url=http://localhost:8080

# === ? Optional: Server Settings ===
server.port=8080
server.address=0.0.0.0
